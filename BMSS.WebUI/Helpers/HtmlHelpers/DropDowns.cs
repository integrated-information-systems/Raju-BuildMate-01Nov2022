namespace BMSS.WebUI.Helpers.HtmlHelpers
{
    public static class DropDowns
    {
        //public static MvcHtmlString DropDownAutoCompleteFor<TModel, TValue>(this HtmlHelper<TModel> html, Expression<Func<TModel, TValue>> expression, IEnumerable<SelectListItem> items = null, string TextName ="", string ValueName = "", object additionalViewData =null)
        //{
        //    var fieldName = ExpressionHelper.GetExpressionText(expression);
        //    var fullBindingName = html.ViewContext.ViewData.TemplateInfo.GetFullHtmlFieldName(fieldName);
        //    var fieldId = TagBuilder.CreateSanitizedId(fullBindingName);
        //    var metadata = ModelMetadata.FromLambdaExpression(expression, html.ViewData);
        //    var value = metadata.Model;
        //    TagBuilder tag = new TagBuilder("select");
        //    tag.Attributes.Add("name", fullBindingName);
        //    tag.Attributes.Add("id", fieldId);
        //    tag.AddCssClass("select2");
            
        //    var validationAttributes = html.GetUnobtrusiveValidationAttributes(fullBindingName, metadata);

        //    foreach (var key in validationAttributes.Keys)
        //        tag.Attributes.Add(key, validationAttributes[key].ToString());

        //    return new MvcHtmlString(tag.ToString(TagRenderMode.Normal));

        //}
    }
}